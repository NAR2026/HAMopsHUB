/* ============================================================
   Sorting
   ============================================================ */
let sortMode = "std-asc";
function sortFlights(list){ const arr = [...list]; switch(sortMode){ case "std-asc": return arr.sort((a,b) => (a.date+ (a.std||"9999")).localeCompare(b.date+(b.std||"9999"))); case "std-desc": return arr.sort((a,b) => (b.date+ (b.std||"0000")).localeCompare(a.date+(a.std||"0000"))); case "date-asc": return arr.sort((a,b) => (a.date+"_"+(a.std||"")).localeCompare(b.date+"_"+(b.std||""))); case "date-desc":return arr.sort((a,b) => (b.date+"_"+(b.std||"")).localeCompare(a.date+"_"+(a.std||""))); case "fn-asc": return arr.sort((a,b) => a.flightNumber.localeCompare(b.flightNumber)); case "fn-desc": return arr.sort((a,b) => b.flightNumber.localeCompare(a.flightNumber)); default: return arr; } }
function sortSelectHtml(){ return `<select class="sort-select" onchange="setSort(this.value)"><option value="std-asc" ${sortMode==="std-asc"?"selected":""}>STD ascending</option><option value="std-desc" ${sortMode==="std-desc"?"selected":""}>STD descending</option><option value="date-asc" ${sortMode==="date-asc"?"selected":""}>Date ascending</option><option value="date-desc" ${sortMode==="date-desc"?"selected":""}>Date descending</option><option value="fn-asc" ${sortMode==="fn-asc"?"selected":""}>Flight # A–Z</option><option value="fn-desc" ${sortMode==="fn-desc"?"selected":""}>Flight # Z–A</option></select>`; }
function setSort(v){ sortMode = v; if(currentTab === "todayTab") renderToday(); else if(currentTab === "allFlightsTab") renderAllFlights(); }
